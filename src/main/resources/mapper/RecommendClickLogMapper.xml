<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.flyfish.learnsphere.mapper.RecommendClickLogMapper">

    <insert id="insert" parameterType="com.flyfish.learnsphere.model.entity.RecommendClickLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO recommend_click_log(user_id, course_id, source, recommend_count, position, create_time)
        VALUES (#{userId}, #{courseId}, #{source}, #{recommendCount}, #{position}, NOW())
    </insert>

    <select id="countClicks" resultType="int">
        SELECT COUNT(*) FROM recommend_click_log WHERE 1=1
        <if test="source != null and source != ''">
            AND source = #{source}
        </if>
    </select>

    <select id="countClickedUsers" resultType="int">
        SELECT COUNT(DISTINCT user_id) FROM recommend_click_log WHERE 1=1
        <if test="source != null and source != ''">
            AND source = #{source}
        </if>
    </select>

    <select id="listClickCountByCourse" resultType="java.util.HashMap">
        SELECT course_id AS courseId, COUNT(*) AS clickCount
        FROM recommend_click_log
        WHERE 1=1
        <if test="source != null and source != ''">
            AND source = #{source}
        </if>
        GROUP BY course_id
        ORDER BY clickCount DESC
        LIMIT #{limit}
    </select>

    <select id="listAll" resultType="com.flyfish.learnsphere.model.entity.RecommendClickLog">
        SELECT id, user_id AS userId, course_id AS courseId, source,
               recommend_count AS recommendCount, position,
               create_time AS createTime
        FROM recommend_click_log
        ORDER BY create_time DESC
    </select>

    <select id="countByUser" resultType="int">
        SELECT COUNT(*) FROM recommend_click_log WHERE user_id = #{userId}
    </select>

</mapper>
