<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.flyfish.learnsphere.mapper.UserMapper">
    <select id="isUserExist" parameterType="java.lang.String" resultType="boolean">
        SELECT
            CASE WHEN COUNT(*) > 0 THEN TRUE ELSE FALSE END
        FROM user
        WHERE username = #{userAccount}
    </select>

    <insert id="save" parameterType="com.flyfish.learnsphere.model.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user(user_account, password, username, email, phone, role_type, status, last_login_time, create_time, update_time)
        VALUES(#{userAccount}, #{password}, #{userName}, #{email}, #{phone}, #{roleType}, #{status}, #{lastLoginTime}, #{createTime}, #{updateTime})
    </insert>

    <select id="getUserByAccount" parameterType="string" resultType="com.flyfish.learnsphere.model.entity.User">
        SELECT * FROM user WHERE user_account = #{userAccount}
    </select>

    <select id="getUserById" parameterType="java.lang.Long" resultType="com.flyfish.learnsphere.model.entity.User">
        SELECT * FROM user WHERE id = #{userId}
    </select>

    <update id="updateUser" parameterType="com.flyfish.learnsphere.model.entity.User">
        UPDATE `user`
        <set>
            <if test="userName != null and userName != ''">
                `username` = #{userName},
            </if>
            <if test="password != null and password != ''">
                `password` = #{password},
            </if>
            <if test="email != null">
                `email` = #{email},
            </if>
            <if test="phone != null">
                `phone` = #{phone},
            </if>
            <if test="roleType != null">
                `role_type` = #{roleType},
            </if>
            <if test="status != null">
                `status` = #{status},
            </if>
            <if test="lastLoginTime != null">
                `last_login_time` = #{lastLoginTime},
            </if>
        </set>
        WHERE `id` = #{id}
    </update>
</mapper>